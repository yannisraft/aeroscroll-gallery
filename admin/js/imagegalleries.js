//console.log("IMAGE GALLERIES");
window.aeroscroll_page = "aeroscroll-gallery-imagegalleries";

window.Logg = (msg, extra) => {
    if(extra)
    {
        console.log(msg, extra);
    } else {
        console.log(msg);
    }
    
}